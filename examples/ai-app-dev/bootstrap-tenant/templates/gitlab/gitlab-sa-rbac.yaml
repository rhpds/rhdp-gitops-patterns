apiVersion: v1
kind: ServiceAccount
metadata:
  name: gitlab-job
  namespace: "{{ .Values.username }}-{{ .Values.namespaceSuffix }}"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: gitlab-job-token-reader-{{ .Values.username }}
  namespace: {{ .Values.gitlab.namespace }}
subjects:
  - kind: ServiceAccount
    name: gitlab-job
    namespace: "{{ .Values.username }}-{{ .Values.namespaceSuffix }}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: gitlab-token-reader
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: gitlab-job-secrets-reader
  namespace: "{{ .Values.username }}-{{ .Values.namespaceSuffix }}"
subjects:
  - kind: ServiceAccount
    name: gitlab-job
    namespace: "{{ .Values.username }}-{{ .Values.namespaceSuffix }}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: edit
