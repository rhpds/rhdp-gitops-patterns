apiVersion: v1
kind: Secret
metadata:
  name: oauth-client
  namespace: {{ .Values.devhub.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  OAUTH_CLIENT_ID: {{ .Values.devhub.oauth.clientId }}
  OAUTH_CLIENT_SECRET: {{ .Values.devhub.oauth.clientSecret }}
