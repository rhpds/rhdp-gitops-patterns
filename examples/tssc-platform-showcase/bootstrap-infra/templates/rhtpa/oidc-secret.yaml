{{- if .Values.rhtpa.enabled }}
---
# CLI OIDC client secret referenced by TrustedProfileAnalyzer.
# Override oidc.cliClientSecret via RHDP secret injection — do not use showcase defaults in prod.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.rhtpa.oidc.cliSecretName }}
  namespace: {{ .Values.rhtpa.namespace }}
  labels:
    {{- include "rhtpa.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "102"
type: Opaque
stringData:
  cli: {{ .Values.rhtpa.oidc.cliClientSecret | quote }}
{{- end }}
