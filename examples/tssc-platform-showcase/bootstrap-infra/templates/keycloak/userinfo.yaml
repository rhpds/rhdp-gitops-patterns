{{- if and .Values.keycloak.enabled .Values.keycloak.userInfo.enabled }}
{{- $url := include "keycloak.url" . }}
{{- $issuer := include "keycloak.issuerUrl" . }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-userinfo-keycloak
  namespace: {{ .Values.keycloak.namespace }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
    demo.redhat.com/userinfo: ""
  annotations:
    argocd.argoproj.io/sync-wave: "54"
data:
  demo_title: {{ .Values.keycloak.userInfo.title | quote }}
  access_instructions: {{ .Values.keycloak.userInfo.instructions | quote }}
  deployment_type: "helm"
  keycloak_url: {{ $url | quote }}
  oidc_issuer: {{ $issuer | quote }}
  oidc_realm: {{ .Values.keycloak.oidc.realm | quote }}
  oidc_frontend_client: {{ .Values.keycloak.oidc.frontendClientId | quote }}
  oidc_cli_client: {{ .Values.keycloak.oidc.cliClientId | quote }}
  demo_username: {{ .Values.keycloak.oidc.demoUser.username | quote }}
  # Demo password — override via values / RHDP secret injection in shared envs
  demo_password: {{ .Values.keycloak.oidc.demoUser.password | quote }}
  trustify_roles: "chicken-user,chicken-manager,chicken-admin"
  trustify_client_scopes: "read:document,create:document,delete:document"
  cluster_domain: {{ .Values.deployer.domain | quote }}
  cluster_api_url: {{ .Values.deployer.apiUrl | quote }}
{{- end }}
