# RHOAI's own internal monitoring stack, independent of cluster UWM — an
# alternate source for model-serving metrics if UWM has issues. Not default.
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: rhoai-prometheus
  namespace: {{ .Values.grafana.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "3"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  datasource:
    access: proxy
    editable: true
    isDefault: false
    jsonData:
      timeInterval: 5s
      tlsSkipVerify: true
    name: RHOAI Prometheus
    type: prometheus
    url: 'https://data-science-monitoringstack-prometheus.redhat-ods-monitoring.svc.cluster.local:9090'
  instanceSelector:
    matchLabels:
      app.kubernetes.io/instance: grafana
