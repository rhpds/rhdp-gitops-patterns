# Generated from rhdp-gitops-patterns/skeleton (v0.1.0)
apiVersion: v1
kind: Service
metadata:
  name: ai-chatbot
  namespace: {{ .Values.username }}-ai-dev
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app: ai-chatbot
spec:
  selector:
    app: ai-chatbot
  ports:
    - port: {{ .Values.aiChatbot.port }}
      targetPort: {{ .Values.aiChatbot.port }}
      protocol: TCP
