# Generated from rhdp-gitops-patterns (gitops-patterns.md - Gitea Operator section)
# Gitea operator uses AllNamespaces install mode via openshift-operators.
# No OperatorGroup needed - openshift-operators already has a global one.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: gitea-operator
  namespace: openshift-operators
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  channel: stable
  installPlanApproval: Automatic
  name: gitea-operator
  source: redhat-gpte-gitea
  sourceNamespace: openshift-marketplace
