{{- if .Values.imageSync.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.imageSync.name }}
  namespace: {{ .Values.cheCluster.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
{{- end }}
